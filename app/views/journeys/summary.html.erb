<div class="flex flex-col gap-4">
  <div class="flex items-center justify-center icon64">
    <%= render "shared/summary" %>
  </div>

  <h1 class="text-center">C'est bientôt l'heure du départ !</h1>

  <%= render "journey_summary_location" %>

  <%= render "journey_summary_members" %>

  <%= render "journey_summary_budget" %>

  <h2 class="text-center pt-4">Pensez à votre logement sur place</h2>

  <%= render "journey_summary_accommodation" %>

  <h2 class="text-center pt-4">Vous allez être bien occupé·e·s !</h2>

  <%= render "journey_summary_activities" %>
</div>
